# Auth

Body of first Auth (top-level H1, occurrence 1).

# Auth

Body of second Auth (top-level H1, occurrence 2).

`anchor: { kind: "heading_path", path: ["Auth"] }` should return
HEADING_AMBIGUOUS with both stable_ids in `candidates`.
